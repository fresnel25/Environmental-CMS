<form method="post" action="{{ path('app_visualisation_delete', {'id': visualisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visualisation.id) }}">
    <button class="btn">Delete</button>
</form>
