<form method="post" action="{{ path('app_dataset_delete', {'id': dataset.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dataset.id) }}">
    <button class="btn">Delete</button>
</form>
