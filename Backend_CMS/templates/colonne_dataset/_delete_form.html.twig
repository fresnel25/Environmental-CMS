<form method="post" action="{{ path('app_colonne_dataset_delete', {'id': colonne_dataset.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ colonne_dataset.id) }}">
    <button class="btn">Delete</button>
</form>
